<div class="offset1">
	<i class="icon-pencil"> <%= link_to "Edit card", edit_deck_card_path(@deck, @card) %></i>
	<i class="icon-trash"> <%= link_to "Delete", deck_card_path(@deck, @card), method: :delete, :confirm => "Are you sure?" %></i>
	
	<br><br>
	<div><%= @card.front %></div>
	<div><%= @card.back %></div>
</div>